# WARDEN Agent Audit

This repository is a public-safe extraction from private WARDEN/AGENTS research.

Rules:
- Do not add secrets, `.env` files, client data, session transcripts, or private WARDEN policy material.
- Keep examples synthetic and repository-review oriented.
- Run `python -m pytest` and `python scripts/check_public_surface.py` before publishing.
- Keep the package dependency-light unless a new dependency is clearly justified.
